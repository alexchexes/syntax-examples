<?php

// TODO:
// @phpstan-ignore variable.undefined
// @phpstan-ignore variable.undefined (Because we are lazy)
// @phpstan-ignore variable.undefined, variable.undefined
// @phpstan-ignore variable.undefined (Because we are lazy), variable.undefined ( Because we are lazy )
// @phpstan-ignore-line
// @phpstan-ignore-next-line

# TODO:
# @phpstan-ignore variable.undefined
# @phpstan-ignore variable.undefined (Because we are lazy)
# @phpstan-ignore variable.undefined, variable.undefined
# @phpstan-ignore variable.undefined (Because we are lazy), variable.undefined ( Because we are lazy )
# @phpstan-ignore-line
# @phpstan-ignore-next-line

/* TODO: */
/* @phpstan-ignore variable.undefined */
/* @phpstan-ignore variable.undefined (Because we are lazy) */
/* @phpstan-ignore variable.undefined, variable.undefined */
/* @phpstan-ignore variable.undefined (Because we are lazy), variable.undefined ( Because we are lazy ) */
/* @phpstan-ignore-line */
/* @phpstan-ignore-next-line */

/** TODO: */
/** @phpstan-ignore variable.undefined */
/** @phpstan-ignore variable.undefined (Because we are lazy) */
/** @phpstan-ignore variable.undefined, variable.undefined */
/** @phpstan-ignore variable.undefined (Because we are lazy), variable.undefined ( Because we are lazy ) */
/** @phpstan-ignore-line */
/** @phpstan-ignore-next-line */

echo $foo; // TODO:
echo $foo; // @phpstan-ignore variable.undefined
echo $foo; // @phpstan-ignore variable.undefined (Because we are lazy)
echo $foo; // @phpstan-ignore variable.undefined, variable.undefined
echo $foo; // @phpstan-ignore variable.undefined (Because we are lazy), variable.undefined ( Because we are lazy )
echo $foo; // @phpstan-ignore-line
echo $foo; // @phpstan-ignore-next-line

echo $foo; /* TODO: */
echo $foo; /* @phpstan-ignore variable.undefined */
echo $foo; /* @phpstan-ignore variable.undefined (Because we are lazy) */
echo $foo; /* @phpstan-ignore variable.undefined, variable.undefined */
echo $foo; /* @phpstan-ignore variable.undefined (Because we are lazy), variable.undefined ( Because we are lazy ) */
echo $foo; /* @phpstan-ignore-line */
echo $foo; /* @phpstan-ignore-next-line */

echo $foo; /** TODO: */
echo $foo; /** @phpstan-ignore variable.undefined */
echo $foo; /** @phpstan-ignore variable.undefined (Because we are lazy) */
echo $foo; /** @phpstan-ignore variable.undefined, variable.undefined */
echo $foo; /** @phpstan-ignore variable.undefined (Because we are lazy), variable.undefined ( Because we are lazy ) */
echo $foo; /** @phpstan-ignore-line */
echo $foo; /** @phpstan-ignore-next-line */

/** 
 * TODO:
 * @phpstan-ignore variable.undefined
 * @phpstan-ignore variable.undefined (Because we are lazy)
 * @phpstan-ignore variable.undefined, variable.undefined
 * @phpstan-ignore variable.undefined (Because we are lazy), variable.undefined ( Because we are lazy )
 * @phpstan-ignore-line
 * @phpstan-ignore-next-line
 */

/*
 * TODO:
 * @phpstan-ignore variable.undefined
 * @phpstan-ignore variable.undefined (Because we are lazy)
 * @phpstan-ignore variable.undefined, variable.undefined
 * @phpstan-ignore variable.undefined (Because we are lazy), variable.undefined ( Because we are lazy )
 * @phpstan-ignore-line
 * @phpstan-ignore-next-line
 */

/*
  TODO:
  @phpstan-ignore variable.undefined
  @phpstan-ignore variable.undefined (Because we are lazy)
  @phpstan-ignore variable.undefined, variable.undefined
  @phpstan-ignore variable.undefined (Because we are lazy), variable.undefined ( Because we are lazy )
  @phpstan-ignore-line
  @phpstan-ignore-next-line
*/

/*
    // TODO:
    // @phpstan-ignore variable.undefined
    // @phpstan-ignore variable.undefined (Because we are lazy)
    // @phpstan-ignore variable.undefined, variable.undefined
    // @phpstan-ignore variable.undefined (Because we are lazy), variable.undefined ( Because we are lazy )
    // @phpstan-ignore-line
    // @phpstan-ignore-next-line
    $foo;
    # TODO:
    # @phpstan-ignore variable.undefined
    # @phpstan-ignore variable.undefined (Because we are lazy)
    # @phpstan-ignore variable.undefined, variable.undefined
    # @phpstan-ignore variable.undefined (Because we are lazy), variable.undefined ( Because we are lazy )
    # @phpstan-ignore-line
    # @phpstan-ignore-next-line
*/
$foo;
// // TODO:
// // @phpstan-ignore variable.undefined
// // @phpstan-ignore variable.undefined (Because we are lazy)
// // @phpstan-ignore variable.undefined, variable.undefined
// // @phpstan-ignore variable.undefined (Because we are lazy), variable.undefined ( Because we are lazy )
// // @phpstan-ignore-line
// // @phpstan-ignore-next-line
$foo;
// /* TODO: */
// /* @phpstan-ignore variable.undefined */
// /* @phpstan-ignore variable.undefined (Because we are lazy) */
// /* @phpstan-ignore variable.undefined, variable.undefined */
// /* @phpstan-ignore variable.undefined (Because we are lazy), variable.undefined ( Because we are lazy ) */
// /* @phpstan-ignore-line */
// /* @phpstan-ignore-next-line */
$foo;
// /** TODO: */
// /** @phpstan-ignore variable.undefined */
// /** @phpstan-ignore variable.undefined (Because we are lazy) */
// /** @phpstan-ignore variable.undefined, variable.undefined */
// /** @phpstan-ignore variable.undefined (Because we are lazy), variable.undefined ( Because we are lazy ) */
// /** @phpstan-ignore-line */
// /** @phpstan-ignore-next-line */
$foo;
// # TODO:
// # @phpstan-ignore variable.undefined
// # @phpstan-ignore variable.undefined (Because we are lazy)
// # @phpstan-ignore variable.undefined, variable.undefined
// # @phpstan-ignore variable.undefined (Because we are lazy), variable.undefined ( Because we are lazy )
// # @phpstan-ignore-line
// # @phpstan-ignore-next-line
$foo;
# # TODO:
# # @phpstan-ignore variable.undefined
# # @phpstan-ignore variable.undefined (Because we are lazy)
# # @phpstan-ignore variable.undefined, variable.undefined
# # @phpstan-ignore variable.undefined (Because we are lazy), variable.undefined ( Because we are lazy )
# # @phpstan-ignore-line
# # @phpstan-ignore-next-line