<?

/** 
 * This is a PHPDoc block that uses some common `markdown` features.
 * 
 * # Heading
 * Normal text
 * 
 * ## Heading with `inline code` and *italic*
 * Normal text
 * 
 * Some unordered list:
 * - Item 1
 * - Item 2
 * Normal text
 * 
 * ### Some ordered list:
 * 1. List
 * 2. Item
 * 
 * Fenced code block default:
 * ```
 * $foo = new Bar();
 * ```
 * 
 * Fenced code block JS:
 * ```js
 * const foo = bar.baz()
 * ```
 * 
 *   Fenced code block with indentation:
 *   ```js
 *   const foo = bar.baz()
 *   ```
 * 
 * Tag inside markdown:
 * ```
 * @private
 * ```
 */
function example() {}
